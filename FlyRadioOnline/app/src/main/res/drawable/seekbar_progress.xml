<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 背景图 -->
    <item android:id="@android:id/background" android:drawable="@drawable/mp3_progress_u"/>

    <!-- 全部进度图-->
    <item android:id="@android:id/secondaryProgress" android:drawable="@drawable/mp3_progress_u"/>

    <!-- 当前进度图-->
    <item android:id="@android:id/progress" android:drawable="@drawable/mp3_progress_d"/>


</layer-list>